using TechnicalTest.Library;
using static TechnicalTest.Library.Helpers;

namespace TechnicalTest.Two
{
    /*
     * We are using a third party library entititled TechnicalTest.Library.
     * This library is included in the project already.
     * We need to create an instance of the "TestTwoRepository" object, and call the Get() Method.
     * There is a bug in the program, so after you instanciate Repository and call Get() you will have to debug.
     * When done you should get a green "Pass" on the console.
     * 
     */

    class Program
    {
        static void Main()
        {
            try
            {
            }
            catch
            {
                Fail();
            }

        }
    }
}
